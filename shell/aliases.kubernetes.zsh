#!/bin/zsh

# aliases for Kubernetes usage

# kubectl

alias kgp="kubectl get pod"
alias kpod="kubectl get pod"
alias kns="kubectl get ns"
alias kei="kubectl edit ing"
alias ksc="kubectl scale deployment"


# 3rd party tools

# Kubernetes file validator
alias kv="kubeval"
# Kubernetes log follower
alias kt="kubetail"
# Kubernetes auditor
alias kspy="kubespy"

# Kubernetes aliases
alias k="kubectl"
alias kg="kubectl get"
alias kget="kubectl get"
alias kpod="kubectl get pod"
alias kns="kubectl get ns"
alias kjob="kubectl get job"
alias klf="kubectl logs -f"
alias kaf="kubectl apply -f"
alias ked="kubectl edit"

