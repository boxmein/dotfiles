#
# Specific aliases for MacOS machines
#

alias ls='ls -CGFh'
alias l='ls -CGFh'
alias sl='ls -CGFh' # choo choo
alias psql="psql"

alias docker-desktop-shell="nc -U ~/Library/Containers/com.docker.docker/Data/debug-shell.sock"

alias routes="netstat -nr"
alias listeners="lsof -nn | grep LISTEN"
